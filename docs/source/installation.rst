Installation
============

To run unisphere:

- Install the code from the github repository:

.. code_block:: console
    https://github.com/UniSpheree/unisphere-app.git

- Install the required packages to run flutter through the terminal:

.. code_block:: console
    pub get

- Run the app using the terminal:
cl
.. code_block:: console
    flutter run
